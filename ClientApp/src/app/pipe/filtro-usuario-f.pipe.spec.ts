import { FiltroUsuarioFPipe } from './filtro-usuario-f.pipe';

describe('FiltroUsuarioFPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUsuarioFPipe();
    expect(pipe).toBeTruthy();
  });
});
