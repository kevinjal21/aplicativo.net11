import { FiltroTramitesPipe } from './filtro-tramites.pipe';

describe('FiltroTramitesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTramitesPipe();
    expect(pipe).toBeTruthy();
  });
});
